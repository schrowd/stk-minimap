# rendered output - but docs/ holds the images the README points at
*.png
!docs/*.png
minimaps/

# replays are the user's own runs, not project files
*.replay

# python
__pycache__/
*.py[cod]
.venv/
venv/

# editors / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# makepkg build artifacts, from testing packaging/aur/PKGBUILD locally
packaging/aur/pkg/
packaging/aur/src/
packaging/aur/*.pkg.tar.*
packaging/aur/*.tar.gz
